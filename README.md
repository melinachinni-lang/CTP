
  # CTP WIREFRAMES (Color palette)

  This is a code bundle for CTP WIREFRAMES (Color palette). The original project is available at https://www.figma.com/design/VkLppagANLMdtcEkGFaCtE/CTP-WIREFRAMES--Color-palette-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  